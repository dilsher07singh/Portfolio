# GitHub personal access token used only at build time by scripts/fetch-github.mjs.
# Requires the read:user scope (classic token) — nothing more.
# Deliberately has no VITE_ prefix: a VITE_-prefixed value is inlined into the
# client bundle by Vite and would be readable by every visitor.
GITHUB_TOKEN=
